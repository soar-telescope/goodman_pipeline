[tox]
env_list =
    py{38, 39, 310, 311, 312}-test{,-cov}
minversion = 4.4.12

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
deps =
    pytest>=6
    pytest-cov
    mock
commands =
    pytest {tty:--color=yes} {posargs}
